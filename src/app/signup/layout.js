export const metadata = {
    title: 'Sign Up | E-PDS Admin Portal',
    description: 'Create a new account to manage the Electronic Public Distribution System.',
};

export default function SignupLayout({ children }) {
    return <>{children}</>;
}
